import React, { Component } from 'react';
import { Authentication } from '../components';

export default class Register extends Component {
  render() {
    return <Authentication mode={false} />;
  }
}
