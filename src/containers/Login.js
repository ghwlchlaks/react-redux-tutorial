import React, { Component } from 'react';
import { Authentication } from '../components';

export default class Login extends Component {
  render() {
    return <Authentication mode={true} />;
  }
}
